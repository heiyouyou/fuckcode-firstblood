<!--pages/login/index/index.wxml-->
<view class='login-index-bg'></view>
<view class='login-index-nav'>
  <view class="{{flag == index ? 'active' : ''}}"
        wx:for="{{nav}}"
        data-i='{{index}}'
        bindtap='onNav'>{{item}}</view>
</view>